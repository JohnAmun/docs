---
title: "Recover a wrongly-sent token"
description: "What to do when a customer sends the wrong token or network — and how to recover funds that didn't auto-credit."
---

"I sent the wrong token" is the single most common crypto-payment support issue. CryptoNow handles most of it automatically, and the rest is recoverable.

{/* Guidde video embed goes here */}

<Info>
  **First, check whether it auto-credited.** If the customer sent a _recognised_ token on the same network (e.g. USDC instead of USDT-ERC20), it's **auto-detected, credited, and forwarded** — nothing to do.
</Info>

<Steps>
  <Step title="Confirm the token wasn't auto-credited">
    {/* Guidde steps */}
  </Step>
  <Step title="Export the client wallet's private key">
    {/* Guidde steps — export the exportable per-client private key */}
  </Step>
  <Step title="Import it to recover the funds">
    {/* Guidde steps — import into MetaMask / Trust / Ctrl and move the funds */}
  </Step>
</Steps>

<Warning>
  Only _unrecognised_ tokens need manual recovery. Recovery is done by exporting the **per-client private key** from that client wallet and importing it into a self-custody wallet — there's no other path, so handle the key carefully.
</Warning>